# Commands (the AI assistant) works without either of these — it falls back to
# deterministic analysis of your scene. Set one to enable generation.
#
# Server-side only. Never expose these with a NEXT_PUBLIC_ prefix.

# GEMINI_API_KEY=
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
